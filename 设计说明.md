# 设计说明

## 一、要解决什么

把一本小说（或任何书）切成「每天读一份」的片段，方便按节奏读完。

**两条硬约束：**

1. 绝不切断句子或段落 —— 每天读到的最后一段必须是完整结束的
2. 每份不超过 6500 字（默认目标 5500 字，约 8 页纸）

## 二、10 页纸大约是多少字

**约 7000 字，合理区间 6000 ~ 8000。**

| 开本 | 每页版面字数 |
| --- | ---: |
| 大 32 开（常见平装小说） | 约 700 |
| 版心较疏的小 32 开 | 约 600 |
| 16 开 | 900 ~ 1100 |

出版行业算「字数」用的是版面字数（每行字数 × 行数），大 32 开约 700 字/页是通行经验值。
反向验证：常见 20 万字的平装小说约 280 ~ 320 页，折算也是每页 650 ~ 700 字。

本工具默认取 5500 字（约 8 页），上限 6500。想按页数换算：`--target` = 每天页数 × 每页字数。

## 三、现有工具为什么不够用

调研了 GitHub 上的同类项目，都是两类做法，都不满足需求：

| 做法 | 代表项目 | 问题 |
| --- | --- | --- |
| 按章节切 | [zh-chapter-splitter](https://github.com/felixchaos/zh-chapter-splitter)、[novel-chapter-splitter](https://github.com/Rinntheor/novel-chapter-splitter)、[txtSplit](https://github.com/Wu-Haitao/txtSplit) | 章节长度极不均匀，有的一章 2000 字、有的 3 万字 |
| 按文件大小切 | 各类 TXT 分割小工具 | 按字节数硬切，会切在句子中间，GBK 下还可能把汉字切坏 |
| 按份数均分 | [TxtSplitTool](https://github.com/luul11/TxtSplitTool) | 反过来倒推份数，控制不了每份多少字 |

另外像 [text-splitter](https://github.com/benbrandt/text-splitter)（★632）、
[chonkie](https://github.com/feyninc/chonkie)（★4772）、LangChain 的分割器，
都是为「喂大模型 / RAG」设计的 —— 它们的默认行为恰恰**允许切在句子中间、允许片段重叠**，
因为对检索无所谓。我们要的是躺着读，读到最后一段必须是完整的一句话，方向一致但约束严得多。

值得借鉴的地方：`zh-chapter-splitter` 的编码识别和章节标题规则融合打分
（零依赖、在 485 万字网文上验证过），`nanfang2000/novel_splitter` 的流式切分思路。

## 四、为什么主干用 DP，而不是语义切分

一开始的设想是：用 HanLP 分句 + Chonkie 的 `SemanticChunker` 做语义边界 + DP 兜底长度。
查证之后否掉了，理由如下。

### 语义阈值那条路方向是反的

Chonkie 官方文档写得很明确：

- `threshold` 默认 **0.8**，含义是「相似度阈值，**越低分出来的块越大**」
- `chunk_size` 默认 2048，是**最大 token 数**，不是目标值
- `delim` 默认 `[". ", "! ", "? ", "\n"]` —— **中文的 `。！？` 一个都不认**

所以把阈值调到 0.55 ~ 0.6 是往「更宽」的方向调，块会被合并得更大，
很快撞上 2048 token 的硬上限 —— 真正决定边界的是 token 上限，语义层等于空转。
想切细应该往 0.85 ~ 0.95 调。默认嵌入模型 `potion-base-32M` 还是英文语料训练的。

更根本的问题是：`SemanticChunker` 的工作单位是句子，切出来的边界经常落在段落内部，
与「不切断段落」直接冲突。必须先吸附到段落边界 —— 吸附之后，语义信息只剩「给段落边界打个分」。

### HanLP 那条路其实只要 4 行正则

Haystack 的 `ChineseDocumentSplitter` 确实是 HanLP 底层，但它是给 RAG 索引用的：
按「N 个句子 / N 个词」切块，不认识段落，也没有字数目标。

而 HanLP 自带的 `split_sentence` 就是 4 行正则（`hanlp/utils/rules.py`），纯标准库、不含模型：

```python
text = re.sub(r'([。！？?])([^”’])', r"\1\n\2", text)
text = re.sub(r'(\.{6})([^”’])', r"\1\n\2", text)
text = re.sub(r'(…{2})([^”’])', r"\1\n\2", text)
text = re.sub(r'([。！？?][”’])([^，。！？?])', r'\1\n\2', text)
```

为了这几行引进 `haystack + hanlp + torch` 一整条依赖链不划算。
而且它每句都 `strip()` 过、不保留偏移量，拿它重建全文会丢缩进。

### 结论：DP 是主干

我们手上有明确的全局目标 —— 每份约 5500 字、尽量落在章末、各天长度别忽长忽短 ——
**这是可以用 DP 精确求解的**。「先语义切、再 DP 修正」是先用软方法产生偏差、
再用硬方法纠回来；既然最终决定权在 DP 手上，中间那层就是纯开销。

语义相似度如果哪天要用，就作为 DP 代价函数里的一项权重（设计上留了接口），
而不是独立的切分阶段。

## 五、三个环节怎么做

### 1. 结构解析（确定性，零依赖）

- 编码识别：UTF-8 / BOM / UTF-16 / GB18030 / GBK / Big5
- EPUB：解析 `container.xml` → OPF → spine，顺带读 nav.xhtml（EPUB3）或 toc.ncx（EPUB2）
  拿真章节名；跳过目录页
- 段落 → 单元：**没有句末标点结尾的段落、图注碎片（`表1-1 …`、`（续）`、`①…`）都不能充当切点**，
  会与下一段合并成一个不可切断的单元
- 分号、冒号不算句末（`如果采纳方案A，会有400人死亡；` 后面还有话）

### 2. DP 选点（主干）

```
dp[j] = min over i ( dp[i] + cost(i, j) )      # i 只遍历长度落在窗口内的候选点

cost(i,j) = 1.0  * ((本份字数 - 5500) / 5500)^2   # 长度偏差，平方惩罚
          + 2.0  * (过短罚分，低于 4000 字时)        # 避免为了凑章末切出很短的份
          - 0.04 * (切点处的场景信号强度)            # 奖：切在自然停顿处
```

场景信号（0 ~ 1）：章首 = 1.0；原文空行 +0.45；时间跳转词（第二天 / 翌日 / 三日后） +0.45。

复杂度 O(n × 窗口)，n ≈ 1,275 个单元，**毫秒级**。
长度允许浮动是这个框架的自然结果：为了对齐章末，某天 4,906 字、某天 6,010 字都是最优解的一部分。

真正的兜底只有一种：单段本身就超过上限，这时才退到段内按句末切，并在报告里提醒。

### 3. 输出

- TXT：每天一份，开头标明天数 / 章节范围 / 字数
- EPUB：每天一章，带目录；**原书插图按原位置打包进去**
- 清单.md：每天的字数、估算 token、章节范围

## 六、跑真书才发现的问题

用《漫步华尔街（原书第12版）》验证（27 万字，EPUB）时暴露了四个问题，
这些问题在一开始自己编的合成样本里**一个都没出现**：

| 问题 | 现象 | 修法 |
| --- | --- | --- |
| 导航目录没被利用 | 章节名退化成「第 1 节」「第 2 节」 | 解析 nav.xhtml / toc.ncx 拿真标题 |
| 目录页混进正文 | 117 行章节名被当成正文塞进第 1 天 | 按「链接数 ≥ 30」识别并跳过 |
| 分号被当成句末 | `方案A…；` / `方案B…` 被拆到两天 | 分号冒号不再算句末 |
| 图注碎片当切点 | 第 39 天断在 `（续）` 上 | 图注碎片与下一段合并 |

## 七、图片处理

《漫步华尔街》有 113 张图（书里的「表1-1」其实都是图片，全书 0 个 `<table>`）。

做法：解析 XHTML 时记录每个 `<img src>` 和 `<image xlink:href>`（SVG 整页图）
出现在「第几个段落之前」，换算成全局段落下标；输出 EPUB 时把图片复制进去，
按原位置插回 `<p class="pic"><img/></p>`。图注文本本来就在，所以图和说明配得上。

TXT 输出是纯文字，只留得下「图3-1 日本股市泡沫：……」这样的图注行。`--no-images` 可以关掉图片。

## 八、已知限制

- **不支持 PDF / DOCX**，会给出提示让你先转换
- **扫描版 EPUB 会明确报错**，提示先做 OCR（见 README）
- 极少数「墙文本」（整本一段到底、没有换行）只能退到按句子切
- 单段超过 6500 字时无法保证「不切断段落」，程序会在报告里提醒
- 章末对齐率不是 100%（真书实测 21/49）。想提高就得放宽字数浮动，牺牲每天的均匀度 ——
  这是取舍，不是 bug

## 九、出处

- Chonkie SemanticChunker 参数：https://docs.chonkie.ai/oss/chunkers/semantic-chunker
- Haystack ChineseDocumentSplitter：https://docs.haystack.deepset.ai/docs/chinesedocumentsplitter
- HanLP 分句实现：https://github.com/hankcs/hanlp/blob/master/hanlp/utils/rules.py
- 出版字数计算：https://www.chushudashi.com/news_show/263.html
- 大 32 开每页约 700 字：https://www.sohu.com/a/824405265_121675507
